import React, { useState } from "react";

const Login = props => {
  const initialUserState = {
    name: "",
    id: "",
  };
  
  return (
    <div className="App">
      Hello Universe!
    </div>
  );
}

export default Login;
